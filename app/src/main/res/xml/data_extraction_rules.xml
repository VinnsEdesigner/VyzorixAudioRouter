<?xml version="1.0" encoding="utf-8"?>
<!--
  Layer 3: opt out of all auto-backup paths for the daemon's data. The
  daemon stores SQLCipher-encrypted state + DeviceSecretStore secrets
  that MUST NOT be replicated to Google's Auto Backup.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
