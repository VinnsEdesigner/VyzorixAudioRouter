// Package placeholder for com.vyzorix.audiorouter.services.ipc.
//
// Layer 3 keeps this package in the source tree (per
// doc/BUILD_ORDER.md §Layer 3 "Stubs only") so subsequent layers can drop
// real implementations in without churning module wiring. The class body
// is intentionally empty — Layers 4+ will replace it with the canonical
// classes named in doc/VyzorixAudioRouter_RepoTree.md.

package com.vyzorix.audiorouter.services.ipc

/** Empty placeholder; will be removed when Layer-4+ classes populate the package. */
internal object IpcPackageStub
